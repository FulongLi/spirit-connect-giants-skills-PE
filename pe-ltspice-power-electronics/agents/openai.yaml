interface:
  display_name: "PE LTspice Simulation"
  short_description: "LTspice transient, small-signal, loop-injection, and convergence debugging"
  default_prompt: "Use $pe-ltspice-power-electronics to debug an LTspice power electronics simulation."

policy:
  allow_implicit_invocation: true
